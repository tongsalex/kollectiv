import React from 'react';
import './Contact.css';
import '../../reset.css';

const Contact = props => (
  <div id="contact-container">
    <div id="sub-contact-container">
    <p>Contact us.</p>
    </div>
  </div>
    );

export default Contact;
